# Build the current checkout instead of pulling the published image.
# Usage: docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build

services:
  vidarch:
    build:
      context: .
      dockerfile: Dockerfile
    image: vidarch:local
