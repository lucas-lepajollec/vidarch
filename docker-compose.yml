services:
  vidarch:
    image: ghcr.io/lucas-lepajollec/vidarch:latest
    ports:
      - "2508:2508"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
    init: true
    restart: unless-stopped
